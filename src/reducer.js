import {change, add, delete} from './actions'

const initialState = {};

export default function (state = initialState, action) {
  switch (action.type) {
    case "change":
      return state;
    case "add":
      return state;
    case "change":
      return state;
    default:
      return state;
  }
}
